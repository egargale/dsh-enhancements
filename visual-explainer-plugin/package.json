{
  "name": "visual-explainer-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Ad-hoc DSH plugin for the visual-explainer skill: deterministic quick-render tool + Web Client chat node.",
  "type": "module",
  "scripts": {
    "sync-assets": "bash scripts/sync-assets.sh",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@deepseek-ai/cordis": "workspace:*",
    "@deepseek-ai/dsh-tools": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}

